# 🚀 Деплой Willow Mini App на Render

## Архитектура проекта

```
Frontend (React + Vite)  →  Render.com (Static Site)
                             ↓
                    Telegram Mini App
                             ↓
Backend (Google Apps Script) → Google Sheets (Database)
```

## 📋 Что нужно

- ✅ Аккаунт на [Render.com](https://render.com)
- ✅ GitHub репозиторий (у вас: `RAZ-AR/Willow-coffee-order`)
- ✅ Google Apps Script уже настроен
- ✅ Telegram Bot создан

---

## 🎯 Пошаговая инструкция

### 1️⃣ Подготовка репозитория

Убедитесь что в репозитории есть:
- ✅ `vite.config.ts` - создан
- ✅ `render.yaml` - создан
- ✅ `package.json` с скриптом `build`

### 2️⃣ Деплой на Render

#### Вариант A: Автоматический деплой через GitHub

1. Откройте [dashboard.render.com](https://dashboard.render.com)
2. Нажмите **"New"** → **"Static Site"**
3. Подключите GitHub репозиторий `RAZ-AR/Willow-coffee-order`
4. Настройте параметры:
   ```
   Name: willow-miniapp
   Branch: main
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```
5. Нажмите **"Create Static Site"**
6. Дождитесь деплоя (2-5 минут)

#### Вариант B: Через render.yaml (рекомендуется)

1. В Render dashboard нажмите **"New"** → **"Blueprint"**
2. Выберите репозиторий `RAZ-AR/Willow-coffee-order`
3. Render автоматически найдет `render.yaml`
4. Нажмите **"Apply"**
5. Дождитесь деплоя

### 3️⃣ Получение URL

После деплоя вы получите URL вида:
```
https://willow-miniapp.onrender.com
```

### 4️⃣ Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота **Willow Coffee**
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Выберите **"Configure Menu Button"**
6. Введите URL: `https://willow-miniapp.onrender.com`
7. Введите название: `Заказать кофе` или `Order Coffee`

### 5️⃣ Проверка работы

1. Откройте бота в Telegram
2. Нажмите кнопку **Menu** (≡) внизу
3. Должно открыться Mini App с меню кофе

---

## 🔄 Автоматические обновления

При каждом `git push` в ветку `main`:
- Render автоматически пересоберет и задеплоит новую версию
- Время деплоя: ~2-5 минут
- Telegram Mini App обновится автоматически при следующем открытии

---

## ⚙️ Backend настройка (Google Apps Script)

Backend уже настроен и работает на:
```
https://script.google.com/macros/s/AKfycbztt8YDXYTAb9xPOSVF5ivBoSc9IJCiywY5iQWBDxPMlBuJ10XhGnoPycVwp0lhshMY/exec
```

Данные хранятся в Google Sheets с таблицами:
- `Cards` - карты лояльности
- `Users` - пользователи
- `Orders` - заказы
- `StarsLog` - история начисления звезд

---

## 🐛 Решение проблем

### Проблема: Build fails на Render

**Решение:**
```bash
# Локально запустите build и проверьте ошибки
npm run build

# Если есть TypeScript ошибки - исправьте их
npm run lint
```

### Проблема: Mini App не открывается

1. Проверьте что URL в BotFather правильный
2. Проверьте что сайт доступен в браузере
3. Проверьте консоль браузера (F12) на ошибки

### Проблема: Заказы не отправляются

1. Проверьте что Google Apps Script URL правильный в `src/constants/index.ts`:
   ```typescript
   export const BACKEND_URL =
     "https://script.google.com/macros/s/.../exec";
   ```
2. Проверьте что в Google Apps Script настроены переменные:
   - `TELEGRAM_TOKEN`
   - `GROUP_CHAT_ID`
   - `SPREADSHEET_ID`

---

## 📊 Мониторинг

### Render Logs
- Откройте сайт в Render dashboard
- Перейдите в **"Logs"** для просмотра деплоя

### Google Apps Script Logs
1. Откройте [Google Apps Script](https://script.google.com)
2. Выберите проект Willow
3. Нажмите **"Executions"** для просмотра логов API запросов

---

## ✅ Финальный чеклист

- [ ] Сайт задеплоен на Render
- [ ] URL получен (https://willow-miniapp.onrender.com)
- [ ] URL настроен в BotFather
- [ ] Mini App открывается в Telegram
- [ ] Меню загружается
- [ ] Заказы отправляются в группу
- [ ] Уведомления приходят клиентам
- [ ] Звезды начисляются корректно

---

## 🎉 Готово!

Теперь ваше приложение работает в продакшене и готово к использованию!

Поддержка: GitHub Issues - https://github.com/RAZ-AR/Willow-coffee-order/issues
