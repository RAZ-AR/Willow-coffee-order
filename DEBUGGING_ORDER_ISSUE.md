# Проблема: Заказы не доходят до сервера

## Обнаруженная проблема

**Симптомы:**
- ✅ Модальное окно "Заказ оформлен" показывается
- ❌ В логах Render нет POST /api/order
- ❌ Уведомления в Telegram не отправляются

**Причина:**
Приложение не получает реальные данные пользователя от Telegram и переходит в **тестовый режим** с `userId = "0000"`. В тестовом режиме API не вызывается, а возвращаются моковые данные.

## Откуда это происходит

### Код в `src/hooks/useTelegramAuth.ts:83`:
```typescript
} else {
  // В dev режиме или для тестирования - фиксированный тестовый ID
  userId = "0000";
  console.log('🧪 Using test mode ID:', userId);
}
```

### Код в `src/hooks/useApi.ts:98-106`:
```typescript
// Тестовый режим
if (currentTgId === "0000") {
  console.log('🧪 Test mode - returning mock order confirmation');
  return {
    ok: true,
    order_id: `o_test_${Date.now()}`,
    card: "0000",
    stars: 5,
    stars_earned: 2
  };
}
```

## Почему Telegram данные не передаются

Telegram WebApp передает данные несколькими способами:

1. **window.Telegram.WebApp.initDataUnsafe.user.id** - основной способ
2. **URL параметр ?tgWebAppData=...** - для Desktop клиента
3. **window.Telegram.WebApp.initData** - закодированная строка

Наш код проверяет все три способа, но если **ни один не работает**, то переходит в тестовый режим.

## Как проверить

### Шаг 1: Откройте Mini App в Telegram

1. Откройте @Willow_coffee_bot в Telegram
2. Нажмите на кнопку меню "☕ Заказать"
3. Откройте DevTools в браузере (если на Desktop)
4. Посмотрите в Console

### Шаг 2: Проверьте логи в консоли

Ищите строку:
```
🧪 Using test mode ID: 0000
```

Если видите эту строку - значит Telegram данные НЕ передаются!

### Шаг 3: Проверьте что именно передает Telegram

Ищите строку:
```
🔍 Telegram detection: {...}
```

Проверьте значения:
- `realTg`: должно быть `true`
- `hasInitData`: должно быть `true`
- `hasUser`: должно быть `true`
- `extractedUserId`: должен быть ваш реальный Telegram ID (не "0000")

## Решения

### Решение 1: Используйте мобильное приложение Telegram

**Почему:** Мобильные клиенты Telegram (iOS/Android) надежнее передают данные в Mini Apps

**Как:**
1. Откройте Telegram на телефоне
2. Найдите @Willow_coffee_bot
3. Нажмите кнопку "☕ Заказать"
4. Сделайте заказ

### Решение 2: Добавьте debug параметр в URL

Для тестирования можно добавить параметр `?debug=1&force=1`:

```
https://willow-coffee-order.onrender.com/?debug=1&force=1
```

Но это **НЕ РЕШИТ** проблему с получением реального user ID!

### Решение 3: Исправить код (РЕКОМЕНДУЕТСЯ)

Нужно изменить логику так, чтобы **всегда генерировался** реальный userId, даже если Telegram данные не передаются.

## Временное решение для тестирования

Можно временно **отключить** тестовый режим и использовать реальный ID даже когда Telegram данные не приходят:

### Вариант A: Использовать localStorage ID

Изменить `src/hooks/useTelegramAuth.ts:82-86`:

```typescript
} else {
  // Генерируем или берем из localStorage уникальный ID
  userId = generateTestUserId();
  console.log('🔄 Using generated/stored user ID:', userId);
}
```

### Вариант B: Убрать тестовый режим из useApi

Изменить `src/hooks/useApi.ts` - **УДАЛИТЬ** все блоки с проверкой `if (currentTgId === "0000")`.

## Проверка после исправления

1. Пересоберите фронтенд: `npm run build`
2. Скопируйте в backend: `rm -rf backend/public && cp -r dist backend/public`
3. Закоммитьте: `git add . && git commit -m "fix: Remove test mode blocking API calls"`
4. Запушьте: `git push origin main`
5. Подождите 2-3 минуты пока Render задеплоит
6. Сделайте заказ через Mini App
7. Проверьте логи Render - должен появиться `POST /api/order`

## Логи для диагностики

Если проблема сохраняется, откройте браузерную консоль (F12) и найдите:

```
🔍 Telegram detection: {...}
```

Скопируйте весь этот объект и отправьте для анализа.
