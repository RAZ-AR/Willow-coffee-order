# 🚀 Руководство по деплою Willow Mini App

## Архитектура

```
Telegram Bot → Webhook → Render.com (Backend) → Supabase (Database)
                         ↓
                    Static Site (Frontend)
```

---

## 📋 Чеклист перед деплоем

- [ ] Аккаунт на [Supabase.com](https://supabase.com)
- [ ] Аккаунт на [Render.com](https://render.com)
- [ ] Telegram Bot создан через @BotFather
- [ ] GitHub репозиторий с кодом

---

## 1️⃣ НАСТРОЙКА SUPABASE (База данных)

### Шаг 1: Создание проекта

1. Откройте [supabase.com](https://supabase.com) и войдите
2. Нажмите "New Project"
3. Заполните:
   - **Name**: `willow-miniapp`
   - **Database Password**: (сохраните!)
   - **Region**: `Europe (Frankfurt)` или ближайший
4. Нажмите "Create new project" (подождите 2-3 минуты)

### Шаг 2: Применение SQL схемы

1. В меню слева выберите **SQL Editor**
2. Нажмите **New Query**
3. Скопируйте весь код из файла `supabase/schema.sql`
4. Вставьте в редактор
5. Нажмите **RUN** (зеленая кнопка)
6. Убедитесь что выполнилось без ошибок ✅

### Шаг 3: Получение credentials

1. В меню слева выберите **Settings** (⚙️) → **API**
2. Скопируйте:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: `eyJhbG...` (длинный токен)
   - **service_role key**: `eyJhbG...` (СЕКРЕТНЫЙ токен!)

⚠️ **ВАЖНО**: `service_role` key держите в секрете!

### Шаг 4: Проверка таблиц

1. В меню слева выберите **Table Editor**
2. Убедитесь что создались таблицы:
   - `users`
   - `menu_items`
   - `orders`
   - `stars_log`
3. В таблице `menu_items` должно быть 6 примеров товаров

✅ **Supabase готов!**

---

## 2️⃣ НАСТРОЙКА TELEGRAM БОТА

### Шаг 1: Создание бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте `/newbot`
3. Введите имя: `Willow Coffee`
4. Введите username: `willow_coffee_bot` (или свой)
5. **Сохраните токен**: `123456789:ABCdef...`

### Шаг 2: Настройка бота

```
/setdescription
Willow Coffee - закажите кофе прямо из Telegram!

/setabouttext
Система заказа кофе с картой лояльности

/setuserpic
(загрузите логотип)
```

### Шаг 3: Получение GROUP_CHAT_ID

1. Создайте группу в Telegram для уведомлений
2. Добавьте туда вашего бота
3. Отправьте любое сообщение в группу
4. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
5. Найдите в ответе:
   ```json
   "chat":{"id":-1234567890, "title":"Willow Staff"}
   ```
6. Скопируйте ID **с минусом**: `-1234567890`

### Шаг 4: GROUP для кассиров (опционально)

Если нужна отдельная группа для кассиров - повторите Шаг 3 для другой группы.

✅ **Telegram бот готов!** (webhook настроим позже)

---

## 3️⃣ ДЕПЛОЙ BACKEND НА RENDER.COM

### Шаг 1: Push кода на GitHub

```bash
cd /path/to/willow-miniapp
git add .
git commit -m "Migration to Render + Supabase"
git push origin main
```

### Шаг 2: Создание Web Service

1. Откройте [render.com](https://render.com) и войдите
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Заполните:
   - **Name**: `willow-backend`
   - **Region**: `Frankfurt (EU Central)`
   - **Branch**: `main`
   - **Root Directory**: `backend`
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: `Free`

### Шаг 3: Настройка Environment Variables

В разделе **Environment** добавьте переменные:

```
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGc... (service_role key!)
TELEGRAM_BOT_TOKEN=123456789:ABCdef...
GROUP_CHAT_ID=-1234567890
CASHIER_GROUP_ID=-9876543210
NODE_ENV=production
PORT=10000
```

### Шаг 4: Deploy

1. Нажмите **Create Web Service**
2. Подождите 3-5 минут
3. После деплоя скопируйте URL: `https://willow-backend.onrender.com`

### Шаг 5: Проверка работы

Откройте в браузере:
```
https://willow-backend.onrender.com/health
```

Должен вернуться:
```json
{
  "ok": true,
  "uptime": 123,
  "timestamp": "..."
}
```

✅ **Backend работает!**

---

## 4️⃣ НАСТРОЙКА TELEGRAM WEBHOOK

Теперь подключим бота к backend:

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://willow-backend.onrender.com/webhook"}'
```

Замените:
- `<ВАШ_ТОКЕН>` → ваш токен от BotFather
- `willow-backend` → ваше имя сервиса

**Ответ должен быть:**
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

### Проверка webhook:

```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

Должно показать:
```json
{
  "url": "https://willow-backend.onrender.com/webhook",
  "pending_update_count": 0
}
```

✅ **Webhook настроен!**

---

## 5️⃣ ДЕПЛОЙ FRONTEND

### Вариант A: Render Static Site

1. В Render нажмите **New +** → **Static Site**
2. Выберите тот же репозиторий
3. Заполните:
   - **Name**: `willow-frontend`
   - **Branch**: `main`
   - **Build Command**: `npm run build`
   - **Publish Directory**: `dist`

4. В **Environment Variables** добавьте:
   ```
   VITE_API_URL=https://willow-backend.onrender.com
   ```

5. Нажмите **Create Static Site**

### Вариант B: Vercel (быстрее)

```bash
npm install -g vercel
cd /path/to/willow-miniapp
vercel
```

Следуйте инструкциям, добавьте env переменную:
```
VITE_API_URL=https://willow-backend.onrender.com
```

### Шаг: Получение URL

После деплоя скопируйте URL frontend:
- Render: `https://willow-frontend.onrender.com`
- Vercel: `https://willow-miniapp.vercel.app`

✅ **Frontend задеплоен!**

---

## 6️⃣ НАСТРОЙКА MINI APP В TELEGRAM

### Шаг 1: Установка Menu Button

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "☕ Заказать",
      "web_app": {
        "url": "https://willow-frontend.onrender.com"
      }
    }
  }'
```

### Шаг 2: Проверка

1. Откройте вашего бота в Telegram
2. Внизу должна появиться кнопка **☕ Заказать**
3. Нажмите на неё - откроется Mini App!

✅ **Mini App работает!**

---

## 7️⃣ ТЕСТИРОВАНИЕ

### Test 1: Регистрация

1. Откройте бота
2. Отправьте `/start`
3. Должно прийти сообщение:
   ```
   Hi, @username!
   Добро пожаловать в Willow! Ваша карта лояльности:
   Карта: #1234
   ⭐ 0
   ```

### Test 2: Открытие Mini App

1. Нажмите кнопку **☕ Заказать**
2. Должно открыться меню
3. В заголовке должна быть ваша карта: `#1234 ⭐ 0`

### Test 3: Заказ

1. Добавьте товар в корзину
2. Нажмите "Оформить заказ"
3. Должно прийти 2 сообщения:
   - Вам: подтверждение заказа
   - В группу: уведомление для персонала

### Test 4: Управление звездами

В группе персонала отправьте:
```
1234 +5
```

Должно прийти:
- В группу: ✅ Stars updated for card #1234: +5 → total 5
- Клиенту: 🎉 Вы получили 5 звезд!

✅ **Всё работает!**

---

## 🐛 TROUBLESHOOTING

### Backend не стартует на Render

**Проблема**: Build failed
- Проверьте что Root Directory = `backend`
- Проверьте что есть `backend/package.json`
- Посмотрите логи в Render Dashboard

### Telegram не получает сообщения

**Проблема**: Уведомления не приходят
- Проверьте `TELEGRAM_BOT_TOKEN` в Render env
- Проверьте webhook: `getWebhookInfo`
- Проверьте логи backend в Render

### Frontend не подключается к API

**Проблема**: CORS errors
- Убедитесь что `VITE_API_URL` правильный
- Проверьте что backend работает: `/health`
- Проверьте CORS в `backend/src/server.js`

### База данных пустая

**Проблема**: Меню не загружается
- Проверьте что SQL schema применилась
- Проверьте Table Editor в Supabase
- Проверьте `SUPABASE_SERVICE_KEY` (не ANON!)

---

## 📊 MONITORING

### Render Logs

```
https://dashboard.render.com → willow-backend → Logs
```

### Supabase Logs

```
Supabase Dashboard → Logs → API Logs
```

### Telegram Webhook Status

```bash
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

---

## 💰 СТОИМОСТЬ

| Сервис | FREE план | Ограничения |
|--------|-----------|-------------|
| Supabase | ✅ FREE | 500MB, 2GB transfer, 50,000 rows |
| Render.com | ✅ FREE | 750 часов/мес, холодный старт |
| Telegram | ✅ FREE | Без ограничений |
| **ИТОГО** | **$0/мес** | Хватит на 100-200 пользователей |

**Когда нужно платить:**
- Render: при > 750 часов/мес ($7/мес)
- Supabase: при > 500MB БД ($25/мес)

---

## 🎉 ГОТОВО!

Теперь ваше приложение полностью работает:

✅ База данных в Supabase
✅ Backend на Render.com
✅ Frontend на Render/Vercel
✅ Telegram бот отправляет уведомления
✅ Система лояльности работает

**Следующие шаги:**
1. Настройте меню в Supabase Table Editor
2. Добавьте реальные цены
3. Загрузите изображения товаров
4. Пригласите персонал в группу
5. Начинайте принимать заказы! 🚀

---

## 📝 ПОЛЕЗНЫЕ ССЫЛКИ

- Supabase Dashboard: https://app.supabase.com
- Render Dashboard: https://dashboard.render.com
- Telegram Bot API: https://core.telegram.org/bots/api
- Mini Apps Guide: https://core.telegram.org/bots/webapps

