# 🛠️ Скрипты для управления

Эта папка содержит полезные скрипты для настройки и управления Telegram ботом.

---

## 📋 Доступные скрипты

### 1. `set-webhook.sh` - Установка webhook

Автоматически настраивает Telegram webhook для вашего бота.

**Использование:**
```bash
bash scripts/set-webhook.sh
```

Скрипт запросит:
- BOT_TOKEN (от @BotFather)
- Backend URL (из Render.com)

**Что делает:**
- Отправляет запрос к Telegram API
- Устанавливает webhook URL
- Показывает результат

---

### 2. `check-webhook.sh` - Проверка webhook

Проверяет статус webhook и показывает подробную информацию.

**Использование:**
```bash
bash scripts/check-webhook.sh
```

Скрипт запросит:
- BOT_TOKEN

**Что показывает:**
- URL webhook
- Количество необработанных сообщений
- Последние ошибки (если есть)
- Рекомендации по исправлению

---

## 🚀 Быстрый старт

### Первая настройка

```bash
# 1. Установите webhook
bash scripts/set-webhook.sh

# 2. Проверьте что всё работает
bash scripts/check-webhook.sh
```

### Регулярная проверка

```bash
# Проверяйте webhook периодически
bash scripts/check-webhook.sh
```

---

## 📝 Примеры

### Установка webhook

```bash
$ bash scripts/set-webhook.sh

🤖 Настройка Telegram Webhook

Введите BOT TOKEN (от @BotFather): 123456789:ABCdef...
Введите URL вашего backend: https://willow-backend.onrender.com

📤 Отправляю запрос к Telegram API...
Webhook URL: https://willow-backend.onrender.com/webhook

📥 Ответ от Telegram:
{
    "ok": true,
    "result": true,
    "description": "Webhook was set"
}

✅ Webhook успешно установлен!
```

### Проверка webhook

```bash
$ bash scripts/check-webhook.sh

🔍 Проверка статуса Telegram Webhook

Введите BOT TOKEN: 123456789:ABCdef...

📤 Запрашиваю информацию о webhook...

📥 Ответ от Telegram:
{
    "ok": true,
    "result": {
        "url": "https://willow-backend.onrender.com/webhook",
        "pending_update_count": 0
    }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Webhook URL: https://willow-backend.onrender.com/webhook
✅ Pending updates: 0 (всё хорошо)
✅ Ошибок нет
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## ⚠️ Требования

- `bash` (macOS/Linux)
- `curl` (обычно уже установлен)
- `python3` (для красивого вывода JSON, опционально)

### Windows

Для Windows используйте:
- Git Bash
- WSL (Windows Subsystem for Linux)
- Или используйте curl команды вручную (см. WEBHOOK_SETUP.md)

---

## 🐛 Troubleshooting

### Скрипт не запускается

**Ошибка:** `Permission denied`

**Решение:**
```bash
chmod +x scripts/set-webhook.sh scripts/check-webhook.sh
```

### curl не найден

**macOS:**
```bash
# curl должен быть предустановлен
curl --version
```

**Linux:**
```bash
sudo apt-get install curl
```

### python3 не найден

Скрипты будут работать и без python3, просто JSON не будет красиво отформатирован.

**Установка (опционально):**

**macOS:**
```bash
brew install python3
```

**Linux:**
```bash
sudo apt-get install python3
```

---

## 📚 Дополнительная информация

Полное руководство по настройке webhook:
👉 [WEBHOOK_SETUP.md](../WEBHOOK_SETUP.md)

Включает:
- Альтернативные способы настройки (браузер, Postman)
- Детальное объяснение каждого поля
- Troubleshooting guide
- Полезные команды

---

## 🔧 Разработка

### Добавить новый скрипт

1. Создайте файл в `scripts/`
2. Добавьте shebang: `#!/bin/bash`
3. Сделайте исполняемым: `chmod +x scripts/your-script.sh`
4. Обновите этот README

### Тестирование

```bash
# Тест установки webhook (используйте тестовый токен!)
bash scripts/set-webhook.sh

# Тест проверки
bash scripts/check-webhook.sh
```

---

## 💬 Поддержка

Проблемы со скриптами?
- Проверьте [WEBHOOK_SETUP.md](../WEBHOOK_SETUP.md)
- Создайте Issue
- Напишите в Telegram

**Happy scripting! 🚀**
