# Проверка переменных окружения на Render

## Проблема
Уведомления Telegram не отправляются при заказах. Нужно проверить переменные окружения.

## Шаг 1: Проверьте переменные окружения на Render

1. Откройте https://dashboard.render.com
2. Выберите ваш сервис "willow-coffee-order"
3. Перейдите в **Environment**
4. Убедитесь, что установлены следующие переменные:

```
TELEGRAM_BOT_TOKEN=8452257685:AAGoz5_czLVTY-ldwq3HonmhyucZ2oUueZA
GROUP_CHAT_ID=-1002479746516
SUPABASE_URL=https://qokwbxpygfmbmlsyhfzy.supabase.co
SUPABASE_SERVICE_KEY=(ваш service role key из Supabase)
NODE_ENV=production
```

## Шаг 2: Получите GROUP_CHAT_ID

Если у вас нет GROUP_CHAT_ID, выполните следующие шаги:

### Вариант 1: Через @RawDataBot

1. Создайте группу в Telegram для получения заказов
2. Добавьте туда вашего бота @willow_coffee_bot
3. Добавьте бота @RawDataBot в ту же группу
4. @RawDataBot автоматически пришлет сообщение с информацией о группе
5. Найдите поле `"chat": { "id": -1234567890 }`
6. Скопируйте этот ID (с минусом!)

### Вариант 2: Через API

1. Отправьте любое сообщение в группу
2. Выполните запрос:

```bash
curl "https://api.telegram.org/bot8452257685:AAGoz5_czLVTY-ldwq3HonmhyucZ2oUueZA/getUpdates"
```

3. Найдите в ответе `"chat":{"id":-1234567890`
4. Скопируйте ID группы

## Шаг 3: Установите переменные на Render

1. В разделе **Environment** нажмите **Add Environment Variable**
2. Добавьте:
   - Key: `GROUP_CHAT_ID`
   - Value: `-1002479746516` (ваш ID группы с минусом)
3. Нажмите **Save Changes**
4. Render автоматически перезапустит сервис

## Шаг 4: Проверьте логи

После перезапуска:

1. Откройте раздел **Logs** в Render Dashboard
2. Сделайте тестовый заказ через Mini App
3. Проверьте логи - вы должны увидеть:

```
📤 Sending notifications...
📤 User ID: 123456789
📨 sendHTMLMessage called: { chatId: 123456789, hasBotToken: true }
📨 Sending to Telegram API...
📨 Telegram API response: { ok: true, result: { ... } }
✅ Message sent to 123456789
```

## Шаг 5: Если уведомления все еще не приходят

### Проверьте права бота в группе:

1. Откройте группу в Telegram
2. Нажмите на название группы → **Manage Group**
3. Перейдите в **Administrators**
4. Проверьте, что ваш бот добавлен как администратор
5. Убедитесь, что у бота есть права:
   - ✅ **Send messages**
   - ✅ **Post messages** (если это канал)

### Проверьте, что бот не заблокирован:

1. Откройте личный чат с ботом
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением

## Шаг 6: Тестирование

После настройки переменных окружения:

1. Откройте Mini App: https://t.me/willow_coffee_bot
2. Нажмите кнопку "☕ Заказать"
3. Добавьте товары в корзину
4. Оформите заказ
5. **Проверьте:**
   - ✅ Личное сообщение от бота с подтверждением заказа
   - ✅ Сообщение в группу персонала с деталями заказа

## Проверка текущих настроек

Вы можете проверить, что переменные установлены правильно, посмотрев логи при запуске сервера:

```
🚀 Willow Backend API started
📡 Server running on port 10000
🌍 Environment: production
⏰ Started at: 2025-12-10T...
✅ All required environment variables are set
```

Если видите `⚠️ Missing environment variables:` - значит не все переменные установлены.
